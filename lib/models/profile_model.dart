class ProfileModel {
  String name, username, bio, gender, image, email, dob;
  ProfileModel({
    this.name,
    this.username,
    this.bio,
    this.gender,
    this.image,
    this.dob,
    this.email,
  });
}